console.log('O console funcionou corretamente!'); // O comando 'console.log' é o mesmo do 'document.write', ou seja, escreve na tela.
let velocidade = 59.9;
console.log(`A velocidade do seu carro é: ${velocidade}Km/h`);

if (velocidade >= 60) { // Condição simples, pois não tem um 'else'.
    console.log(`Você ultrapassou a velocidade permitida. MULTADO!`)
}

console.log('Dirija sempre usando cinto de segurança!');
